# OA API Tests Configuration
# Base URL for the Gateway - override with OA_TEST_BASE_URL env var
base.url=http://localhost:8088

# Test account identifiers (values passed via env var or secure channel)
# Do NOT commit real passwords here
test.admin.username=admin
test.employee.username=employee

# Connection settings
connect.timeout=10000
read.timeout=15000
